apiVersion: v1
kind: Secret
metadata:
  name: ecommerce-cod-secrets
  namespace: ecommerce-cod
type: Opaque
stringData:
  # Database credentials
  DATABASE_URL: "postgresql://user:password@postgres-service:5432/ecommerce_cod"
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "ecommerce_cod"

  # Redis credentials
  REDIS_PASSWORD: "changeme"

  # JWT secrets
  JWT_SECRET: "your-super-secret-jwt-key-change-this"
  JWT_REFRESH_SECRET: "your-super-secret-refresh-key-change-this"

  # Grafana
  GRAFANA_ADMIN_PASSWORD: "changeme"

---
# Note: In production, use sealed secrets or external secret management
# Do not commit actual secrets to version control
# This is a template file only
